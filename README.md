# SQL_BootCamp

Это `BootCamp` содержит задания и их решения в папке `src` в каждой папке есть реадме и в папке `materials` есть нужные файлы. Можете ознакомиться а возможно если вам интересно попробовать решить самим).
